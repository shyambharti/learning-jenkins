pipeline {
    agent any

    stages {
        stage('Hello Manoj and Tapas') {
            steps {
                echo 'Hello World'
            }
            steps {
                           echo "${date}"
                   }
        }
    }
}
